{
  "name": "nemophila-backend",
  "version": "1.0.0",
  "dependencies": {
    "ws": "^7.4.4"
  },
  "devDependencies": {
    "@types/ws": "^7.4.0"
  },
  "scripts": {
    "build": "yarn clean && tsc -p ./tsconfig.json",
    "clean": "rm -rf ./build",
    "start": "node build/index.js"
  }
}
